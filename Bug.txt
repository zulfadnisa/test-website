Bug:
https://absensi.dpmptsp.cirebonkab.go.id. - Error Forbidden (403)



---------- Note -------
1. Tambahkan pengecekan konten (keyword seperti "error", "maintenance")
if response.status_code == 200:
    if any(k in response.text.lower() for k in ["maintenance", "under construction", "error"]):
        results.append(f"⚠️ Aktif tapi isi error: {url}")
    else:
        results.append(f"✅ Aktif: {url}")
2. Bug di code -> kirim email
3. hasil report
4. Simpan Status Sebelumnya → Deteksi Perubahan (Up ⇄ Down)
5. Bisa tambahkan statistik "berubah dari status sebelumnya" (kalau kamu ingin sistem pintar). Dashboard atau grafik historis

6. Simpan hasil HTTPS/HTTP yang sukses sebelumnya, jadi next run gak harus coba dua-duanya lagi


ISSUE :
1. Bot blocked
- unakan library yang bisa simulasi browser (kalau perlu)
- httpx — lebih powerful dari requests
- undetected-chromedriver — jalanin Chrome headless (berat)
- PING
from ping3 import ping
import requests

host = "example.com"
url = f"https://{host}"

# Step 1: Ping test
if ping(host, timeout=2) is None:
    print(f"[PING] {host} is DOWN")
else:
    print(f"[PING] {host} is UP")



- Menggunakan Rotating Proxies (berbayar kayaknya)
- Headless Browser (Playwright, Puppeteer, Selenium)
- Bypass Tantangan: cloudscraper
- Menanganan cloudscraper Failure
- Bypass SafeLine